CREATE TABLE mainuser(
    id serial primary key,
    firstname VARCHAR(30),
    lastname VARCHAR(30),
    username VARCHAR(30),
    password VARCHAR(30)
    );
CREATE TABLE ClassGroup(
    id serial primary key,
    name varchar(30)
    );